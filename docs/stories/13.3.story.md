# Story 13.3: Integration Verification & Cleanup

## Status
Approved

## Story
**As a** developer,
**I want** all application flows verified against SeaweedFS and MinIO references cleaned up,
**so that** the system is fully operational on the new storage backend with no dead references.

## Acceptance Criteria
1. All existing unit tests pass with SeaweedFS backend
2. End-to-end flow works: upload book → trigger AI processing → serve data via API key
3. Publisher asset upload/download/delete works
4. Teacher material upload/download works
5. Trash soft-delete → restore works
6. Presigned URL generation and browser access works
7. Bundle creation (fget/fput operations) works
8. Production docker-compose updated for SeaweedFS
9. Deployment runbook updated with SeaweedFS configuration
10. Admin panel build and tests pass

## Tasks / Subtasks

- [ ] Task 1: Run existing test suite (AC: 1, 10)
  - [ ] Run `pytest` in `apps/api/` — all tests pass
  - [ ] Run `npm run build` in `apps/admin-panel/` — passes
  - [ ] Run `npm test -- --run` in `apps/admin-panel/` — passes
  - [ ] Document any test failures and fix

- [ ] Task 2: End-to-end book flow (AC: 2)
  - [ ] Upload a book ZIP via admin panel or API
  - [ ] Trigger AI processing
  - [ ] Wait for completion
  - [ ] Fetch book data, AI metadata, vocabulary via API key
  - [ ] Verify all data accessible and correctly formatted

- [ ] Task 3: Storage operations verification (AC: 3, 4, 5, 6, 7)
  - [ ] Upload publisher asset (logo) → verify download works
  - [ ] Delete publisher asset → verify removed from SeaweedFS
  - [ ] Upload teacher material → verify listed and downloadable
  - [ ] Soft-delete a book → verify moved to trash bucket
  - [ ] Restore from trash → verify back in publishers bucket
  - [ ] Generate presigned URL → verify accessible in browser
  - [ ] Create a bundle (if templates exist) → verify download works

- [ ] Task 4: Update production docker-compose (AC: 8)
  - [ ] Update `infrastructure/docker-compose.prod.yml` with SeaweedFS services
  - [ ] Mirror the same SeaweedFS setup from dev docker-compose
  - [ ] Add proper restart policies for SeaweedFS services
  - [ ] Remove any MinIO references from prod compose

- [ ] Task 5: Update deployment runbook (AC: 9)
  - [ ] Update `docs/deployment-runbook.md`:
    - Replace MinIO references with SeaweedFS
    - Update environment variables section
    - Update service startup order (SeaweedFS master → volume → filer → s3)
    - Update backup/restore section for SeaweedFS
    - Update health check verification
    - Update troubleshooting table

- [ ] Task 6: Remove MinIO-specific references (cleanup)
  - [ ] Remove old `minio_data` volume from docker-compose
  - [ ] Remove MinIO console port references (9090) from docs
  - [ ] Update README files if they reference MinIO
  - [ ] Verify `.env.example` has correct SeaweedFS defaults
  - [ ] Run `ruff check` and `npm run lint` — clean

## Dev Notes

### Key Files
- `infrastructure/docker-compose.yml` — already updated in 13.1
- `infrastructure/docker-compose.prod.yml` — needs SeaweedFS services
- `docs/deployment-runbook.md` — needs MinIO → SeaweedFS updates
- `apps/api/app/routers/health.py` — already checks via Minio SDK (works with SeaweedFS)
- `apps/api/README.md` — may reference MinIO
- `apps/admin-panel/README.md` — may reference MinIO

### Previous Story Context
- Story 13.1 set up SeaweedFS in dev docker-compose and updated config defaults
- Story 13.2 migrated data from MinIO to SeaweedFS
- No application code was changed in either story — all changes were infrastructure

### Testing Approach
- Tests use mocked MinIO clients — they don't actually connect to storage
- The E2E verification (Tasks 2-3) is the real integration test
- Use curl commands from the deployment runbook for systematic verification

### Important
- If any test fails, determine if it's a SeaweedFS incompatibility or a pre-existing issue
- Presigned URLs are the most likely area for subtle differences — test thoroughly
- Bundle creation uses `fget_object` / `fput_object` (file-based S3 operations) — verify these work

## Testing

### Test Location
- API: `apps/api/tests/`
- Admin Panel: `apps/admin-panel/src/__tests__/`

### Testing Standards
- All existing tests must pass
- E2E verification serves as integration test
- Document any SeaweedFS-specific quirks found

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-29 | 0.1 | Initial story draft | Bob (SM Agent) |

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

